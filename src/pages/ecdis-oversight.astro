---
import NarrowAngle from "@/components/NarrowAngle.astro";
---

<NarrowAngle
  variant="ecdis-oversight"
  tier="Tier 3 · Compliance"
  h1="Track every third-party data source your models touch."
  lead="External Consumer Data and Information Sources — ECDIS — are called out explicitly by NY DFS and NAIC. If your AI pulls from them, you are accountable for them. Most carriers cannot list the sources, the vendors, or the contracts."
  whyTitle="What we're watching."
  whyBody="ECDIS tracking lives at the inference path. Post-hoc inventories miss sources that are only touched under certain query conditions. The only reliable method is fingerprinting the data as it enters the prompt — which is why most carriers don't have a complete list yet."
  watchTitle="Five recurring findings on ECDIS reviews."
  watchPoints={[
    "The list of active sources changes constantly. Sources added mid-quarter without governance review are the most common finding in examinations.",
    "Contract references must link to the specific ECDIS in use. 'We have a contract with vendor X' is not sufficient if X supplies multiple data products.",
    "Consumer disclosure of material ECDIS elements is required under NY DFS. Most disclosures are generic and do not survive regulator review.",
    "Do-not-use lists should be enforced at the inference layer, not only at ingestion. Data that was ingested once gets called again.",
    "DORA's ICT third-party register overlaps with ECDIS tracking. Build one register that satisfies both; maintaining two is expensive and always drifts.",
  ]}
  newsletterPrompt="Every Tuesday: which ECDIS issues regulators are flagging, the new data sources carriers are onboarding, and where disclosures are holding or failing."
/>
