---
import NarrowAngle from "@/components/NarrowAngle.astro";
---

<NarrowAngle
  variant="hallucination-guardrail"
  tier="Tier 2 · AI operations"
  h1="Stop a made-up coverage answer before it reaches a customer."
  lead="Most hallucinations in insurance are quantitative: an invented coverage limit, a wrong deductible, a reassuring clause that doesn't exist. They sound confident, they pass a fast reader, and they are the easiest kind to catch with a gate."
  whyTitle="What we're watching."
  whyBody="The gate is deterministic: every number must match your pricing API, every coverage assertion must trace to a policy document. The LLM is a composition layer, not a source of truth. Treating it that way is the single largest safety upgrade a carrier can make this quarter."
  watchTitle="Five checks worth building today."
  watchPoints={[
    "Hard-check numbers against your pricing API synchronously. A 50ms cost prevents a misquote — an acceptable trade for the risk it removes.",
    "Every coverage claim must cite a document chunk. No citation, no delivery. This rule alone removes most of the visible failure modes.",
    "Disclosures cannot live in the prompt — the model drops them under pressure. Post-process injection is more reliable and survives every model update.",
    "Safe fallback text matters as much as the block itself. Design it for the customer, not the engineer. A blunt block is a worse UX than no bot.",
    "Blocking mode is best turned on per agent after thirty days of observe-only traces, not on day one. The baseline matters.",
  ]}
  newsletterPrompt="Every Tuesday: which hallucination patterns showed up in production this week across carriers — and what the gate caught."
/>
