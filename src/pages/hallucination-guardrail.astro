---
import NarrowAngle from "@/components/NarrowAngle.astro";
---

<NarrowAngle
  variant="hallucination-guardrail"
  umbrellaHref="/synthetic.html"
  umbrellaLabel="Monitoring angle"
  eyebrow="For AI-ops, compliance, product"
  h1Before="An invented €1M coverage limit."
  h1Em="Caught before delivery."
  lead="A young renter in Lyon asked a home-insurance agent if her contract covered death at home. The model invented a one-million-euro life clause. Phidea flagged it on three policy checks — scope mismatch, ACPR IDD advice-boundary breach, unsupported quantitative assertion — blocked it, served a fallback, logged the incident. Production deployment."
  sectionEyebrow="The policy gate"
  sectionH2="Three layers. Any flag withholds the response."
  sectionBody="Grounding check: every substantive claim must trace to a document in your corpus. Price-accuracy check: every number fails closed against your pricing API. Hallucination classifier: trained on insurance-specific failure patterns, runs on every output. Most carriers start in observe-only, cross the blocking threshold after 30 days of clean traces."
  faqs={[
    { q: "What's the false-positive rate on the classifier?", a: "Under 2% at production threshold on the tier-1 European carrier deployment. Tuneable per agent and per LOB." },
    { q: "What happens to the customer when a response is blocked?", a: "Safe fallback routes to a scripted reply or, if configured, to a human agent. No dead end." },
    { q: "Can you catch French-language hallucinations?", a: "Yes. Classifier is multilingual and tuned for FR, EN, DE, IT, ES insurance vocabulary." },
  ]}
  ctaH2="See a blocked hallucination, live."
  ctaBody="Fifteen minutes. We run a probe designed to trigger a hallucination and walk you through the gate decision and the trace."
  ctaButton="Request a demo →"
/>
