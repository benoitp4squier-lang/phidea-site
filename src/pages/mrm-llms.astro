---
import NarrowAngle from "@/components/NarrowAngle.astro";
---

<NarrowAngle
  variant="mrm-llms"
  tier="Tier 4 · Model risk"
  h1="Govern LLMs under the MRM framework you already have."
  lead="SR 11-7 and PRA SS1/23 were written for statistical models — stable inputs, documented validation, challenger-model benchmarking. LLMs break several of those assumptions. Your MRM organization should still own them; the framework needs an extension, not a replacement."
  whyTitle="What we're watching."
  whyBody="The extension is mostly about telemetry. An LLM cannot be validated once; it has to be validated continuously. The controls in SR 11-7 and SS1/23 translate cleanly if you give them the data stream they need — prompts, responses, tool calls, policy outcomes."
  watchTitle="Five extensions worth making."
  watchPoints={[
    "Effective-challenge documentation is generated from live traces, not prepared for a committee before release. The committee reads the trace.",
    "Challenger models are vendor alternatives (Claude vs GPT vs Gemini), benchmarked hourly against the same prompts. Version numbers are not benchmarks.",
    "Material-model-change review fires automatically when behavioral fingerprint shifts, not only when the provider publishes a version number.",
    "Validation artifacts should include the prompt library, the probe library, and the policy pack version. All three are required to reconstruct the test.",
    "Risk tiering drives cadence, not just scope. Tier-1 LLMs need hourly evidence; Tier-3 can be weekly. Annual for LLMs is not credible.",
  ]}
  newsletterPrompt="Every Tuesday: how MRM teams are extending SR 11-7 and SS1/23 to LLMs — the controls that translate, and the ones that need new definitions."
/>
