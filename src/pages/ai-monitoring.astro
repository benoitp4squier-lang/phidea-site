---
import NarrowAngle from "@/components/NarrowAngle.astro";
---

<NarrowAngle
  variant="ai-monitoring"
  tier="Tier 2 · AI operations"
  h1="Know what your AI agents said last night."
  lead="Most carriers running LLMs in production cannot tell you what their agents answered between 2am and 6am. The trace exists, partially, in the provider's logs — for 30 days. That gap is not sustainable once a regulator asks for a year's worth."
  whyTitle="What we're watching."
  whyBody="AI observability is not the same as application monitoring. You need prompt, response, tool calls, and policy decisions captured at the same layer. A request/response log cannot tell you why the agent said what it said — only that something happened."
  watchTitle="Five things most carriers are missing."
  watchPoints={[
    "Provider-side logs from OpenAI or Anthropic are typically retained 30 days. Regulators ask for twelve months. Your compliance calendar and the default retention window do not agree.",
    "Tool calls are the most important signal and the most often omitted. Log every function call the agent made, with arguments and result.",
    "Grounding score — whether the response actually cited your corpus — is what catches most hallucinations before production.",
    "Policy decisions should be captured even when no policy fired. The absence of a flag is itself evidence, and regulators treat it as such.",
    "Latency per layer (model, tool, policy) matters more than aggregate latency. Drift hides in layers the top-line p99 cannot see.",
  ]}
  newsletterPrompt="Every Tuesday: the observability gaps we see carriers closing — and the ones they're still surprised by when a regulator asks."
/>
