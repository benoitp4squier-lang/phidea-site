---
import NarrowAngle from "@/components/NarrowAngle.astro";
---

<NarrowAngle
  variant="audit-bundle"
  tier="Tier 2 · AI operations"
  h1="Have a regulator bundle ready, every quarter."
  lead="A regulator request for AI evidence is not a surprise. The cadence is published. What's surprising is how few carriers can produce the bundle on demand — most assemble it reactively, from scattered systems, under deadline."
  whyTitle="What we're watching."
  whyBody="The bundle is an artifact, not a report. It contains the signed sessions, the policy pack version, the ECDIS log, the flagged-incident list — cryptographically linked. If you had to build it tomorrow from the systems you already run, where would you start?"
  watchTitle="Five things every bundle should contain."
  watchPoints={[
    "NAIC data call format specifies the fields. Match that schema from day one; retrofitting is expensive and introduces errors.",
    "NY DFS expects the material elements of the AIS and the consumer-disclosure check. Both are runtime artifacts — not documents prepared quarterly.",
    "EIOPA supervisory data requires a governance snapshot tied to the period reviewed. A current-state snapshot is a frequent audit finding.",
    "DORA's incident form requires classification within a specific window. Map your incident taxonomy before you need it, not after.",
    "Every export should itself be logged. 'What did you send last time?' is the second-order question every regulator asks in a follow-up.",
  ]}
  newsletterPrompt="Every Tuesday: which regulators are asking what, the bundle formats carriers are building against, and where exports failed review."
/>
