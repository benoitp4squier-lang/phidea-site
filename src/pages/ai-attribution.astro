---
import NarrowAngle from "@/components/NarrowAngle.astro";
---

<NarrowAngle
  variant="ai-attribution"
  tier="Tier 1 · Distribution"
  h1="Trace each AI conversation to the policy it bound."
  lead="Measuring a channel means knowing what it bound. For AI, that requires tagging each assistant session with enough context to survive the jump from chat to quote tool to policy admin system."
  whyTitle="What we're watching."
  whyBody="Each hop strips context. Most attribution stacks were built for web clicks — not for conversations that happen inside a model's context window and then hand off to a website. The shape of the data is wrong; the fix is a tagging schema that assumes loss at every step."
  watchTitle="Five moves that preserve attribution."
  watchPoints={[
    "Deterministic session IDs survive across agents, quote tools, and policy admin if passed as opaque tokens. UUIDs are fine; timestamps are not.",
    "Personas matter. A first-home buyer and an annuity renewer convert at wildly different rates. Collapse them and the channel looks noisy.",
    "The question asked is a feature. Log it, then join it to binding later — that join is where AI attribution becomes actionable.",
    "Duck Creek and Guidewire accept UTM-style params today. Salesforce and HubSpot need custom-field mapping but cost an afternoon.",
    "Cohort reports by surface × LOB × persona reveal where AI is working, not just that it is working. Aggregate alone flattens the signal.",
  ]}
  newsletterPrompt="Every Tuesday: what carriers measuring AI as a distribution channel are seeing — conversion rates, persona winners, attribution wins and losses."
/>
